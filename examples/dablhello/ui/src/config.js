const continuousUpdate = false;

// DABL config
const dablConfig = {
  continuousUpdate,
  tokens: {
    // Copy token for Alice from DABL website
    "Alice": "",
    "Bob": ""
  },
  parties: {
    Alice: "",  // Copy Party ID from DABL website
    Bob: ""
  }
}

const config = dablConfig
export default config;
